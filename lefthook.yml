pre-push:
  piped: true
  assert_lefthook_installed: true

  jobs:
    - name: Lint 
      run: make lint
    
    - name: Validate OpenAPI
      run: make validate-api
    
commit-msg:
  jobs:
    - name: Commit message check
      run: ./scripts/commit-message-check.sh {0}